import dashboard from './dashboard';
import user from './user';
import manages from './manages';
import order from './order';
import ticket from './ticket';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, manages, order, ticket, user]
};


export default menuItems;
